# Todo

## Task

* add `ngxs` in project
* save date on add item (readonly)
* add `deadline` param on add, edit 
* if deadline expired in `todo` and `progress` should be invalid and marked, but if `complete` should remove from `complete` column
* create own calendar component(material calendar cannot be used)
* there must be validation so that it is impossible to put a deadline on a non-working day

[Calendar Api](https://isdayoff.ru/)
